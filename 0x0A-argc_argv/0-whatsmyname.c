#include <stdio.h>

/**
 * main - Prints the name of the program
 * @argc: argument counter
 * @argv: Array for keeping arguments supplied
 * Return: 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
